# pyproject.toml
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gas-simulation"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["gas_simulation*"]
